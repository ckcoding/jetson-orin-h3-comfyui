# 实测性能与能力对比（Jetson Orin 24G）

> 全部数据为实机实测（2026-09-13/14），可复现脚本见 `benchmarks/cap_final.sh`。

## 1. 推理速度总表

| 模型 | 量化 | 权重大小 | 架构 | 层 offload | 生成速度 | 提示词处理 |
|---|---|---|---|---|---|---|
| Qwen3.8-27B | Q4_K_M | 16.8G | qwen35 密集 + GatedDeltaNet | 66/66 → CUDA | **7.58 tok/s** (132 ms/tok) | ~250 tok/s |
| Qwen3.6-35B-A3B | UD-IQ4_XS | 17.73G | qwen35moe 混合 MoE | 65/65 → CUDA | **31.73 tok/s** (31.5 ms/tok) | ~16 tok/s¹ |
| Z-Image-Turbo | Q4_K_M GGUF | 4.7G | S3-DiT 6B | 全量 → CUDA | 1024² 图像 **89.8s**（8步） | — |

¹ MoE 的提示词处理 61 ms/tok 是单条短提示样本，长上下文吞吐更高。

**关键洞察**：密集 27B 每 token 必读 16.8G 权重 → 内存带宽墙（157GB/s 理论 ≈ 9.3 tok/s 顶，
实测 7.58 = 墙的 81-90%，**已到顶**）。A3B MoE 每 token 只读 ~3G 激活权重 → 突破墙体，4.2× 提速。

## 2. 能力对比（同题对决，temperature 0.3）

### 数学多步推理（正确答案 22）
| | 过程 | 答案 | token 数 |
|---|---|---|---|
| 27B-dense | 紧凑三步：37-9=28 → +60=88 → 88÷4 | **22 ✓** | 212 |
| 35B-A3B-MoE | 缜密分步 + 自我验证（Step1-7 + 复核除数）| **22 ✓** | 800 |

两台全对。27B 风格精炼；35B-A3B 思考更全面（含防呆验证），
但注意它是**重思考模型**——num_predict 给太小（如 300）会让思考吃掉全部额度、
最终答案出不来，建议 ≥800。

### 逻辑推理（Tom 怕水？）
两台均正确回答（三段论 + 猫属前提），27B 两句精炼，35B-A3B 结构化推导。

### 知识问答（解释 MoE）
两台均能准确解释专家分工/门控路由/稀疏激活。35B-A3B 回答更结构化。

### 代码（回文函数）
两台均能生成正确实现（`re.sub` 清洗 + 双向比较）。35B-A3B 带 thinking 过程。

### 中文创意（三词小诗）
两台均可完成；创意判断留给读者（样例已存档）。

## 3. 总耗时趣味事实

MoE 单 token 快 4.2×，但**重思考模型想得也多**——同一道数学题
两台出答案的墙钟时间打平（~25-28s）。差异体现在：
- 简单任务（短思考）：MoE 时间 ×4 碾压
- 复杂任务（长思考）：耗时趋同，MoE 换来更稳的过程质量

## 4. 内存共存策略（24G 统一内存）

| 组合 | 可行性 |
|---|---|
| qwen36 驻留 (17.7G) 单独 | ✓ |
| qwen38 驻留 (16.8G) 单独 | ✓ |
| qwen36 + z-image 生成峰值 (~12G) | ✗ 互斥，Ollama 5 分钟闲置自动卸载，或 `keep_alive:0` 手动 |
| 两个 LLM 同时驻留 | ✗（17.7+16.8 > 24）|

切换成本：模型从盘载回 ~2-3 分钟（vblkdev 虚拟盘 ~100-200MB/s）。

## 5. 网络与传输实测

| 路径 | 速度 |
|---|---|
| 本机 → 服务器 (ssh cat, LAN) | ~30-39 MB/s |
| HuggingFace 直连（本机） | ~5.5 MiB/s（限速）|
| hf-mirror.com（本机） | ~5.5 MiB/s（同被限）|
| aria2c 校验下载 17.73G | 55 min |

⚠️ 教训：aria2c 断点续传恢复时产生了**全零空壳文件**（大小正确、内容全零），
且 sha256 双端互验无法发现（两端同为坏拷贝）。修复：下载时加
`--checksum=sha-256=<官方hash>`（HF API tree 接口的 `lfs.oid` 字段可取），
坏块自动重下；下载后必检 GGUF 头（magic + general.architecture）。
